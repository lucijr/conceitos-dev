/**
 * Babel: Convert (transpiler) React code to a code that the browse understands.
 * Webpack: For each type of file (.js, .css, .png) it converts the code in a different way.
 * 
 * Loaders: babel-loader, css-loader, image-loader.
 * 
 * Commands: yarn add @babel/core @babel/preset-env @babel/preset-react @babel-cli webpack webpack-cli
 * 
 * 
 */
